package(default_visibility = ["//:__subpackages__"])

load("@npm//@bazel/typescript:index.bzl", "ts_library")

ts_library(
    name = "olark",
    srcs = glob(
        ["*.ts"],
        exclude = ["spec.ts"],
    ),
    module_name = "@intf/olark",
    deps = [
        "@npm//@types/node",
        "@npm//angular",
    ],
)
