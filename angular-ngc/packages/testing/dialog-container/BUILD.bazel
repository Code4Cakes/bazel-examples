package(default_visibility = ["//:__subpackages__"])

load("@npm//@angular/bazel:index.bzl", "ng_module")

COMMON_DEPS = [
    "@npm//@angular/core",
    "@npm//@angular/common",
]

ng_module(
    name = "dialog-container",
    srcs = glob(
        ["*.ts"],
        exclude = ["*.spec.ts"],
    ),
    module_name = "@intf/testing/dialog-container",
    deps = COMMON_DEPS,
)
